Place custom images, GIFs, and audio files here, kitty-style =^..^=.

Map filenames in `manifest.json`. Paths begin below `/reactions/`.

```json
"kiss": { "image": "kiss.gif", "audio": "kiss.mp3" }
```

Kitty Mesh still runs with no files here. The live source viewer and built-in tones keep purring.
